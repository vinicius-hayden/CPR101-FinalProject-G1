#ifndef _CONVERTING_H_
#define _CONVERTING_H_

// Include the necessary header files for standard input/output, string manipulation, and memory allocation
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

// Declare a function prototype for the function named 'converting'
void converting(void);

// End the header file conditional inclusion
#endif
